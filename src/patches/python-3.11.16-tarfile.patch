--- Python-3.11.16/Lib/tarfile.py	2026-03-03 04:52:57.000000000 +0400
+++ Python-3.11.16/Lib/tarfile.py	2026-08-11 12:12:24.578373960 +0400
@@ -2680,7 +2680,7 @@
                     os.lchown(targetpath, u, g)
                 else:
                     os.chown(targetpath, u, g)
-            except OSError as e:
+            except (OSError, OverflowError) as e:
                 raise ExtractError("could not change owner") from e
 
     def chmod(self, tarinfo, targetpath):
