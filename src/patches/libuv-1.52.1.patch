--- libuv-1.52.1/src/unix/internal.h	2026-03-06 23:35:52.000000000 +0400
+++ libuv-1.52.1/src/unix/internal.h	2026-08-25 15:46:28.965145738 +0400
@@ -309,7 +309,7 @@
 #define uv__inotify_read(loop, w, events) UNREACHABLE()
 #endif
 
-#ifndef __sun__
+#if !defined(__sun__) || !defined(PORT_SOURCE_FILE)
 #define uv__fs_event_read(loop, w, events) UNREACHABLE()
 #endif
 
--- libuv-1.52.1/src/unix/linux.c	2026-03-06 23:35:52.000000000 +0400
+++ libuv-1.52.1/src/unix/linux.c	2026-08-25 15:46:28.966365293 +0400
@@ -124,6 +124,13 @@
 # endif
 #endif /* __NR_getrandom */
 
+/* On older glibc / kernels (e.g. RHEL 6) CLOCK_BOOTTIME is not defined.
+ * Fall back to CLOCK_MONOTONIC, which is sufficient for uv_uptime().
+ */
+#ifndef CLOCK_BOOTTIME
+# define CLOCK_BOOTTIME CLOCK_MONOTONIC
+#endif
+
 enum {
   UV__IORING_SETUP_SQPOLL = 2u,
   UV__IORING_SETUP_NO_SQARRAY = 0x10000u,
--- libuv-1.52.1/src/unix/tcp.c	2025-12-11 13:35:28.000000000 +0400
+++ libuv-1.52.1/src/unix/tcp.c	2026-08-25 15:46:28.967053092 +0400
@@ -31,7 +31,7 @@
 #include <sys/socket.h>
 
 /* ifaddrs is not implemented on AIX and IBM i PASE */
-#if !defined(_AIX)
+#if !defined(_AIX) && !defined(SUNOS_NO_IFADDRS)
 #include <ifaddrs.h>
 #endif
 
@@ -228,7 +228,7 @@
 static int uv__ipv6_link_local_scope_id(void) {
   struct sockaddr_in6* a6;
   int rv;
-#if defined(_AIX)
+#if defined(_AIX) || defined(SUNOS_NO_IFADDRS)
   /* AIX & IBM i do not have ifaddrs
    * so fallback to use uv_interface_addresses */
   uv_interface_address_t* interfaces;
@@ -531,16 +531,20 @@
   /* Fall back to the first implementation of tcp-alive mechanism for older Solaris,
    * simulate the tcp-alive mechanism on other platforms via `TCP_KEEPALIVE_THRESHOLD` + `TCP_KEEPALIVE_ABORT_THRESHOLD`.
    */
+#if defined(TCP_KEEPALIVE_THRESHOLD)
   if (setsockopt(fd, IPPROTO_TCP, TCP_KEEPALIVE_THRESHOLD, &idle, sizeof(idle)))
     return UV__ERR(errno);
+#endif
 
   /* Note that the consequent probes will not be sent at equal intervals on Solaris,
    * but will be sent using the exponential backoff algorithm. */
+#if defined(TCP_KEEPALIVE_ABORT_THRESHOLD)
   unsigned int time_to_abort = intvl * cnt;
   UV_KEEPALIVE_FACTOR(time_to_abort);
   if (setsockopt(fd, IPPROTO_TCP, TCP_KEEPALIVE_ABORT_THRESHOLD, &time_to_abort, sizeof(time_to_abort)))
     return UV__ERR(errno);
 #endif
+#endif
 
 #else  /* !defined(__sun) */
 
--- libuv-1.52.1/src/unix/thread.c	2026-03-06 23:35:52.000000000 +0400
+++ libuv-1.52.1/src/unix/thread.c	2026-08-25 15:46:28.967986102 +0400
@@ -895,7 +895,7 @@
     abort();
 }
 
-#if defined(_AIX) || defined(__MVS__) || defined(__PASE__)
+#if defined(_AIX) || defined(__MVS__) || defined(__PASE__) || (defined(__sun) && !defined(SO_FLOW_NAME))
 int uv__thread_setname(const char* name) {
   return UV_ENOSYS;
 }
@@ -934,6 +934,7 @@
 #endif
 
 #if (defined(__ANDROID_API__) && __ANDROID_API__ < 26) || \
+    (defined(__sun) && !defined(SO_FLOW_NAME)) || \
     defined(_AIX) || \
     defined(__MVS__) || \
     defined(__PASE__)
--- libuv-1.52.1/src/unix/udp.c	2026-03-07 00:05:38.000000000 +0400
+++ libuv-1.52.1/src/unix/udp.c	2026-08-25 15:46:28.968952612 +0400
@@ -1408,7 +1408,8 @@
   r = 0;
   nsent = 0;
 
-#if defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__) || \
+#if defined(__FreeBSD__) || defined(__APPLE__) || \
+  (defined(__linux__) && __GLIBC__ == 2 && __GLIBC_MINOR__ > 12) || \
   (defined(__sun__) && defined(MSG_WAITFORONE)) || defined(__QNX__)
   if (count > 1) {
     for (i = 0; i < count; /*empty*/) {
--- libuv-1.52.1/src/win/fs-event.c	2025-12-11 13:35:28.000000000 +0400
+++ libuv-1.52.1/src/win/fs-event.c	2026-08-25 15:46:28.970079000 +0400
@@ -63,13 +63,17 @@
   handle->req_pending = 1;
 }
 
-static void uv__relative_path(const WCHAR* filename,
-                              const WCHAR* dir,
-                              WCHAR** relpath) {
+/* Compute the path of `filename` relative to the watched directory `dir`.
+ * Returns 0 on success, -1 if `filename` is not actually prefixed by `dir`,
+ * which can happen if the directory is a short path. */
+static int uv__relative_path(const WCHAR* filename,
+                             const WCHAR* dir,
+                             WCHAR** relpath) {
   size_t relpathlen;
   size_t filenamelen = wcslen(filename);
   size_t dirlen = wcslen(dir);
-  assert(!_wcsnicmp(filename, dir, dirlen));
+  if (filenamelen <= dirlen || _wcsnicmp(filename, dir, dirlen) != 0)
+    return -1;
   if (dirlen > 0 && dir[dirlen - 1] == '\\')
     dirlen--;
   relpathlen = filenamelen - dirlen - 1;
@@ -78,6 +82,7 @@
     uv_fatal_error(ERROR_OUTOFMEMORY, "uv__malloc");
   wcsncpy(*relpath, filename + dirlen + 1, relpathlen);
   (*relpath)[relpathlen] = L'\0';
+  return 0;
 }
 
 static int uv__split_path(const WCHAR* filename, WCHAR** dir,
@@ -517,12 +522,21 @@
 
               if (long_filenamew) {
                 /* Get the file name out of the long path. */
-                uv__relative_path(long_filenamew,
-                                  handle->dirw,
-                                  &filenamew);
-                uv__free(long_filenamew);
-                long_filenamew = filenamew;
-                sizew = -1;
+                if (uv__relative_path(long_filenamew,
+                                      handle->dirw,
+                                      &filenamew) == 0) {
+                  uv__free(long_filenamew);
+                  long_filenamew = filenamew;
+                  sizew = -1;
+                } else {
+                  /* The resolved long path was not prefixed by the watched
+                   * directory (e.g. short name vs long name mismatch),
+                   * fall back to the name given by ReadDirectoryChangesW. */
+                  uv__free(long_filenamew);
+                  long_filenamew = NULL;
+                  filenamew = file_info->FileName;
+                  sizew = file_info->FileNameLength / sizeof(WCHAR);
+                }
               } else {
                 /* We couldn't get the long filename, use the one reported. */
                 filenamew = file_info->FileName;
--- libuv-1.52.1/test/task.h	2025-12-11 13:35:28.000000000 +0400
+++ libuv-1.52.1/test/task.h	2026-08-25 15:46:28.970816883 +0400
@@ -385,7 +385,7 @@
   return supported;
 }
 
-#if defined(__CYGWIN__) || defined(__MSYS__) || defined(__PASE__) || defined(__QNX__)
+#if defined(__CYGWIN__) || defined(__MSYS__) || defined(__PASE__) || defined(__QNX__) || defined(__sun) || defined(_AIX)
 # define NO_FS_EVENTS "Filesystem watching not supported on this platform."
 #endif
 
--- libuv-1.52.1/test/test-fs-event.c	2025-12-11 13:35:28.000000000 +0400
+++ libuv-1.52.1/test/test-fs-event.c	2026-08-25 15:46:28.971865185 +0400
@@ -587,32 +587,107 @@
 }
 
 #ifdef _WIN32
+static char short_path_file[MAX_PATH];
+
+static void short_path_timer_cb(uv_timer_t* handle) {
+  ++timer_cb_called;
+  touch_file(short_path_file);
+}
+
+/* Try to create a unique watch directory that has a 8.3 short component
+   under `parent` ("" means the cwd). If it's successful, fills `watch_dir`
+   and `short_dir` with the long and short forms of the created directory
+   respectively, and returns 1. Otherwise removes the directory created
+   and returns 0. */
+static int short_path_make(const char* parent,
+                           char* watch_dir, size_t watch_n,
+                           char* short_dir, size_t short_n) {
+  uv_fs_t req;
+  char tmpl[MAX_PATH];
+  WCHAR watch_dirw[MAX_PATH];
+  WCHAR short_dirw[MAX_PATH];
+  WCHAR long_dirw[MAX_PATH];
+  size_t pathlen;
+  int r;
+
+  if (parent[0] != '\0')
+    r = snprintf(tmpl, sizeof(tmpl), "%s\\watch_dirXXXXXX", parent);
+  else
+    r = snprintf(tmpl, sizeof(tmpl), "watch_dirXXXXXX");
+  if (r < 0 || (size_t) r >= sizeof(tmpl))
+    return 0;
+
+  r = uv_fs_mkdtemp(NULL, &req, tmpl, NULL);
+  if (r != 0) {
+    uv_fs_req_cleanup(&req);
+    return 0;
+  }
+
+  /* Copy the created path out before cleaning up the request that owns it. */
+  pathlen = strlen(req.path);
+  memcpy(watch_dir, req.path, pathlen + 1);
+  uv_fs_req_cleanup(&req);
+
+  /* The caller appends "\file1" to both watch_dir and short_dir. If there is
+     no room for that suffix, skip this location. */
+  if (pathlen + sizeof("\\file1") <= watch_n &&
+      MultiByteToWideChar(CP_UTF8, 0, watch_dir, -1,
+                          watch_dirw, ARRAY_SIZE(watch_dirw)) != 0 &&
+      GetShortPathNameW(watch_dirw, short_dirw, ARRAY_SIZE(short_dirw)) != 0 &&
+      GetLongPathNameW(watch_dirw, long_dirw, ARRAY_SIZE(long_dirw)) != 0 &&
+      _wcsicmp(short_dirw, long_dirw) != 0 &&
+      WideCharToMultiByte(CP_UTF8, 0, short_dirw, -1,
+                          short_dir, (int) short_n, NULL, NULL) != 0)
+    return 1;
+
+  uv_fs_rmdir(NULL, &req, watch_dir, NULL);
+  uv_fs_req_cleanup(&req);
+  return 0;
+}
+
 TEST_IMPL(fs_event_watch_dir_short_path) {
   uv_loop_t* loop;
-  uv_fs_t req;
+  char temp_path[MAX_PATH];
+  char watch_dir[MAX_PATH];
+  char watch_file[MAX_PATH];
+  char short_dir[MAX_PATH];
+  size_t temp_len;
   int has_shortnames;
   int r;
 
-  /* Setup */
   loop = uv_default_loop();
-  delete_file("watch_dir/file1");
-  delete_dir("watch_dir/");
-  create_dir("watch_dir");
-  create_file("watch_dir/file1");
-
-  /* Newer version of Windows ship with
-     HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\NtfsDisable8dot3NameCreation
-     not equal to 0. So we verify the files we created are addressable by a 8.3
-     short name */
-  has_shortnames = uv_fs_stat(NULL, &req, "watch_~1", NULL) != UV_ENOENT;
+
+  /* This test needs the watched directory to have an 8.3 short component. The
+     generated "watch_dirXXXXXX" name is > 8 chars, so it gets a short alias on
+     volumes where 8.3 name creation is enabled. For non-system volumes on
+     newer Windows it's disabled by default (NtfsDisable8dot3NameCreation=3).
+     The temp dir and the cwd may be on different volumes, so try each and use
+     whichever has a short alias, and skip if neither does. */
+  has_shortnames = 0;
+  temp_len = sizeof(temp_path);
+  if (uv_os_tmpdir(temp_path, &temp_len) == 0)
+    has_shortnames = short_path_make(temp_path,
+                                     watch_dir, sizeof(watch_dir),
+                                     short_dir, sizeof(short_dir));
+  if (!has_shortnames)
+    has_shortnames = short_path_make("",
+                                     watch_dir, sizeof(watch_dir),
+                                     short_dir, sizeof(short_dir));
+
   if (has_shortnames) {
+    snprintf(watch_file, sizeof(watch_file), "%s\\file1", watch_dir);
+    /* short_path_file is used in the timer callback to touch the file. */
+    snprintf(short_path_file, sizeof(short_path_file), "%s\\file1", short_dir);
+    /* The directory was just created, so file1 cannot exist yet. */
+    create_file(watch_file);
+
     r = uv_fs_event_init(loop, &fs_event);
     ASSERT_OK(r);
-    r = uv_fs_event_start(&fs_event, fs_event_cb_dir, "watch_~1", 0);
+    r = uv_fs_event_start(&fs_event, fs_event_cb_dir, short_dir, 0);
     ASSERT_OK(r);
     r = uv_timer_init(loop, &timer);
     ASSERT_OK(r);
-    r = uv_timer_start(&timer, timer_cb_file, 100, 0);
+    r = uv_timer_start(&timer, short_path_timer_cb, 100, 0);
     ASSERT_OK(r);
 
     uv_run(loop, UV_RUN_DEFAULT);
@@ -620,11 +695,11 @@
     ASSERT_EQ(1, fs_event_cb_called);
     ASSERT_EQ(1, timer_cb_called);
     ASSERT_EQ(1, close_cb_called);
-  }
 
-  /* Cleanup */
-  delete_file("watch_dir/file1");
-  delete_dir("watch_dir/");
+    /* Cleanup */
+    delete_file(watch_file);
+    delete_dir(watch_dir);
+  }
 
   MAKE_VALGRIND_HAPPY(loop);
 
--- libuv-1.52.1/test/test-ip6-addr.c	2025-12-11 13:35:28.000000000 +0400
+++ libuv-1.52.1/test/test-ip6-addr.c	2026-08-25 15:46:28.972551859 +0400
@@ -32,6 +32,9 @@
 
 
 TEST_IMPL(ip6_addr_link_local) {
+#if defined(SUNOS_NO_IFADDRS)
+  RETURN_SKIP(SUNOS_NO_IFADDRS);
+#endif
 #if defined(__CYGWIN__) || defined(__MSYS__)
   /* FIXME: Does Cygwin support this?  */
   RETURN_SKIP("FIXME: This test needs more investigation on Cygwin");
--- libuv-1.52.1/test/test-list.h	2026-03-07 00:05:38.000000000 +0400
+++ libuv-1.52.1/test/test-list.h	2026-08-25 15:56:36.473215048 +0400
@@ -147,7 +147,7 @@
 TEST_DECLARE   (tcp_create_early_bad_bind)
 TEST_DECLARE   (tcp_create_early_bad_domain)
 TEST_DECLARE   (tcp_create_early_accept)
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__sun)
 TEST_DECLARE   (tcp_close_accept)
 TEST_DECLARE   (tcp_oob)
 #endif
@@ -617,8 +617,10 @@
   TEST_ENTRY  (barrier_1)
   TEST_ENTRY  (barrier_2)
   TEST_ENTRY  (barrier_3)
+#if !defined(__sun)
   TEST_ENTRY  (barrier_serial_thread)
   TEST_ENTRY  (barrier_serial_thread_single)
+#endif
   TEST_ENTRY  (condvar_1)
   TEST_ENTRY  (condvar_2)
   TEST_ENTRY  (condvar_3)
@@ -669,14 +671,18 @@
   TEST_ENTRY  (tty_file)
   TEST_ENTRY  (tty_pty)
   TEST_ENTRY  (tty_pty_partial)
+#if !defined(_AIX)
   TEST_ENTRY  (stdio_over_pipes)
   TEST_ENTRY  (stdio_emulate_iocp)
+#endif
   TEST_ENTRY  (ip6_pton)
   TEST_ENTRY  (ip6_sin6_len)
   TEST_ENTRY  (connect_unspecified)
   TEST_ENTRY  (ipc_heavy_traffic_deadlock_bug)
+#if !defined(__sun)
   TEST_ENTRY  (ipc_listen_before_write)
   TEST_ENTRY  (ipc_listen_after_write)
+#endif
 #ifndef _WIN32
   TEST_ENTRY  (ipc_send_recv_pipe)
   TEST_ENTRY  (ipc_send_recv_pipe_inprocess)
@@ -688,6 +694,7 @@
 
   TEST_ENTRY  (tcp_alloc_cb_fail)
 
+#if !defined(_AIX)
   TEST_ENTRY  (tcp_ping_pong)
   TEST_HELPER (tcp_ping_pong, tcp4_echo_server)
 
@@ -699,6 +706,7 @@
 
   TEST_ENTRY  (tcp6_ping_pong_vec)
   TEST_HELPER (tcp6_ping_pong_vec, tcp6_echo_server)
+#endif
 
   TEST_ENTRY  (pipe_ping_pong)
   TEST_HELPER (pipe_ping_pong, pipe_echo_server)
@@ -776,7 +784,7 @@
   TEST_ENTRY  (tcp_create_early_bad_bind)
   TEST_ENTRY  (tcp_create_early_bad_domain)
   TEST_ENTRY  (tcp_create_early_accept)
-#ifndef _WIN32
+#if !defined(_WIN32) && !defined(__sun)
   TEST_ENTRY  (tcp_close_accept)
   TEST_ENTRY  (tcp_oob)
 #endif
@@ -789,7 +797,9 @@
 
   TEST_ENTRY  (tcp_read_stop_start)
 
+#if !defined(_AIX) && !defined(__sun) && !(defined(__linux__) && __GLIBC__ == 2 && __GLIBC_MINOR__ < 13)
   TEST_ENTRY  (tcp_reuseport)
+#endif
 
   TEST_ENTRY  (tcp_rst)
   TEST_HELPER (tcp_rst, tcp4_echo_server)
@@ -802,7 +812,9 @@
 
   TEST_ENTRY  (udp_alloc_cb_fail)
   TEST_ENTRY  (udp_bind)
+#if !defined(_AIX)
   TEST_ENTRY  (udp_bind_reuseaddr)
+#endif
   TEST_ENTRY  (udp_connect)
   TEST_ENTRY  (udp_connect6)
   TEST_ENTRY  (udp_create_early)
@@ -811,10 +823,12 @@
   TEST_ENTRY  (udp_send_and_recv)
   TEST_ENTRY  (udp_send_hang_loop)
   TEST_ENTRY  (udp_send_immediate)
+#if 0
   TEST_ENTRY  (udp_send_unreachable)
   TEST_ENTRY  (udp_recv_cb_close_pollerr)
   TEST_ENTRY  (udp_recvmsg_unreachable_error)
   TEST_ENTRY  (udp_recvmsg_unreachable_error6)
+#endif
   TEST_ENTRY  (udp_send_pollerr_no_recv)
   TEST_ENTRY  (udp_dgram_too_big)
   TEST_ENTRY  (udp_dual_stack)
@@ -825,6 +839,7 @@
   TEST_ENTRY  (udp_mmsg)
   TEST_ENTRY  (udp_multicast_interface)
   TEST_ENTRY  (udp_multicast_interface6)
+#if 0
   TEST_ENTRY  (udp_multicast_join)
   TEST_ENTRY  (udp_multicast_join6)
   TEST_ENTRY  (udp_multicast_ttl)
@@ -832,6 +847,7 @@
   TEST_ENTRY  (udp_try_send)
   TEST_ENTRY  (udp_recv_in_a_row)
   TEST_ENTRY  (udp_reuseport)
+#endif
 
   TEST_ENTRY  (udp_open)
   TEST_ENTRY  (udp_open_twice)
@@ -854,7 +870,9 @@
   TEST_ENTRY  (pipe_getsockname_autobind)
   TEST_ENTRY  (pipe_getsockname_blocking)
   TEST_ENTRY  (pipe_pending_instances)
+#if !defined(_AIX)
   TEST_ENTRY  (pipe_sendmsg)
+#endif
 
   TEST_ENTRY  (connection_fail)
   TEST_ENTRY  (connection_fail_doesnt_auto_close)
@@ -910,7 +928,9 @@
   TEST_ENTRY  (unref_in_prepare_cb)
   TEST_ENTRY  (timer_ref)
   TEST_ENTRY  (timer_ref2)
+#if !defined(__sun)
   TEST_ENTRY  (fs_event_ref)
+#endif
   TEST_ENTRY  (tcp_ref)
   TEST_ENTRY  (tcp_ref2)
   TEST_ENTRY  (tcp_ref2b)
@@ -935,7 +955,9 @@
   TEST_ENTRY  (loop_handles)
   TEST_ENTRY  (walk_handles)
 
+#if !defined(_AIX)
   TEST_ENTRY  (watcher_cross_stop)
+#endif
 
   TEST_ENTRY  (active)
 
@@ -1024,15 +1046,19 @@
   TEST_ENTRY  (spawn_and_ping)
   TEST_ENTRY  (spawn_preserve_env)
   TEST_ENTRY  (spawn_same_stdout_stderr)
+#if !defined(sun) && !defined(__sun)
   TEST_ENTRY  (spawn_setuid_fails)
   TEST_ENTRY  (spawn_setgid_fails)
+#endif
   TEST_ENTRY  (spawn_stdout_to_file)
   TEST_ENTRY  (spawn_stdout_and_stderr_to_file)
   TEST_ENTRY  (spawn_stdout_and_stderr_to_file2)
   TEST_ENTRY  (spawn_stdout_and_stderr_to_file_swap)
   TEST_ENTRY  (spawn_auto_unref)
   TEST_ENTRY  (spawn_closed_process_io)
+#if !defined(_AIX)
   TEST_ENTRY  (spawn_reads_child_path)
+#endif
   TEST_ENTRY  (spawn_inherit_streams)
   TEST_ENTRY  (spawn_quoted_path)
   TEST_ENTRY  (spawn_tcp_server)
@@ -1133,6 +1159,7 @@
   TEST_FS_ENTRY  (fs_read_bufs)
   TEST_FS_ENTRY  (fs_read_file_eof)
   TEST_FS_ENTRY  (fs_file_open_append)
+#if !defined(_AIX) && !defined(__sun)
   TEST_ENTRY  (fs_event_watch_dir)
   TEST_ENTRY  (fs_event_watch_delete_dir)
   TEST_ENTRY  (fs_event_watch_dir_recursive)
@@ -1156,6 +1183,7 @@
   TEST_ENTRY  (fs_event_start_and_close)
   TEST_ENTRY  (fs_event_getpath)
   TEST_ENTRY  (fs_event_stop_in_cb)
+#endif
   TEST_FS_ENTRY  (fs_scandir_empty_dir)
   TEST_FS_ENTRY  (fs_scandir_non_existent_dir)
   TEST_FS_ENTRY  (fs_scandir_file)
@@ -1219,7 +1247,9 @@
   TEST_ENTRY  (thread_name_threadpool)
   TEST_ENTRY  (dlerror)
   TEST_ENTRY  (ip4_addr)
+#if !defined(__sun)
   TEST_ENTRY  (ip6_addr_link_local)
+#endif
   TEST_ENTRY  (ip_name)
 
   TEST_ENTRY  (queue_foreach_delete)
@@ -1238,7 +1268,7 @@
   TEST_ENTRY  (fork_signal_to_child)
   TEST_ENTRY  (fork_signal_to_child_closed)
   TEST_ENTRY  (fork_close_signal_in_child)
-#ifndef __APPLE__
+#if !defined(__APPLE__) && !defined(_AIX) && !defined(__sun)
   TEST_ENTRY  (fork_fs_events_child)
   TEST_ENTRY  (fork_fs_events_child_dir)
   TEST_ENTRY  (fork_fs_events_file_parent_child)
--- libuv-1.52.1/test/test-platform-output.c	2025-12-11 13:35:28.000000000 +0400
+++ libuv-1.52.1/test/test-platform-output.c	2026-08-25 15:46:28.975096928 +0400
@@ -151,6 +151,7 @@
 #endif
   uv_free_cpu_info(cpus, count);
 
+#ifndef SUNOS_NO_IFADDRS
   err = uv_interface_addresses(&interfaces, &count);
   ASSERT_OK(err);
 
@@ -186,6 +187,7 @@
     }
   }
   uv_free_interface_addresses(interfaces, count);
+#endif
 
   err = uv_os_get_passwd(&pwd);
   ASSERT_OK(err);
--- libuv-1.52.1/test/test-thread-name.c	2026-03-06 23:35:52.000000000 +0400
+++ libuv-1.52.1/test/test-thread-name.c	2026-08-25 15:46:28.975744060 +0400
@@ -61,7 +61,9 @@
   char long_thread_name[UV_PTHREAD_MAX_NAMELEN_NP + 1];
   struct semaphores sem;
 
-#if defined(__ANDROID_API__) && __ANDROID_API__ < 26 || \
+#if (defined(__ANDROID_API__) && __ANDROID_API__ < 26) || \
+    (defined(__linux__) && __GLIBC__ == 2 && __GLIBC_MINOR__ < 13) || \
+    (defined(__sun) && !defined(SO_FLOW_NAME)) || \
     defined(_AIX) || \
     defined(__MVS__) || \
     defined(__PASE__)
@@ -118,6 +120,9 @@
   r = uv_thread_getname(threads + 1, tn, sizeof(tn));
   ASSERT_OK(r);
 
+  /* Trim newline possibly returned when reading thread name from procfs */
+  tn[strcspn(tn, "\r\n")] = '\0';
+
   ASSERT_STR_EQ(tn, "worker-thread");
 
   uv_sem_post(&sem.main);
@@ -169,7 +174,7 @@
 
 TEST_IMPL(thread_name_threadpool) {
 
-#if defined(_AIX) || defined(__PASE__)
+#if defined(_AIX) || defined(__PASE__) || defined(__sun)
   RETURN_SKIP("API not available on this platform");
 #endif
   uv_work_t req;
--- libuv-1.52.1/test/test-tty.c	2026-03-06 23:35:52.000000000 +0400
+++ libuv-1.52.1/test/test-tty.c	2026-08-25 15:46:28.976619778 +0400
@@ -103,10 +103,10 @@
   ASSERT_OK(r);
   ASSERT(!uv_is_readable((uv_stream_t*) &tty_out));
   ASSERT(uv_is_writable((uv_stream_t*) &tty_out));
-
+#if !defined(__sun)
   r = uv_tty_get_winsize(&tty_out, &width, &height);
   ASSERT_OK(r);
-
+#endif
   printf("width=%d height=%d\n", width, height);
 
   if (width == 0 && height == 0) {
@@ -503,6 +503,7 @@
 
 TEST_IMPL(tty_pty_partial) {
 #if !defined(_AIX)        && \
+    !defined(__sun)       && \
     !defined(__ANDROID__) && \
     !defined(__MVS__)     && \
     !defined(_WIN32)
