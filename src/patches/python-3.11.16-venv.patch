--- Python-3.11.16/Lib/venv/scripts/posix/activate.csh	2026-03-03 04:52:57.000000000 +0400
+++ Python-3.11.16/Lib/venv/scripts/posix/activate.csh	2026-08-11 12:24:21.738436657 +0400
@@ -14,10 +14,14 @@
 setenv PATH "$VIRTUAL_ENV/"__VENV_BIN_NAME__":$PATH"
 
 
-set _OLD_VIRTUAL_PROMPT="$prompt"
+if ($?prompt) then
+    set _OLD_VIRTUAL_PROMPT="$prompt"
+endif
 
 if (! "$?VIRTUAL_ENV_DISABLE_PROMPT") then
-    set prompt = __VENV_PROMPT__"$prompt"
+    if ($?prompt) then
+        set prompt = __VENV_PROMPT__"$prompt"
+    endif
     setenv VIRTUAL_ENV_PROMPT __VENV_PROMPT__
 endif
 
