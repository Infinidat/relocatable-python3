diff --git a/Makefile.msvc b/Makefile.msvc
index 352ea65..b61e6bf 100644
--- a/Makefile.msvc
+++ b/Makefile.msvc
@@ -121,6 +121,7 @@ install : force
 	-mkdir $(prefix)
 	-mkdir $(includedir)
 	$(INSTALL_DATA) include\iconv.h $(includedir)\iconv.h
+	$(INSTALL_DATA) windows\unistd.h $(includedir)\unistd.h
 
 installdirs : force
 	cd libcharset
