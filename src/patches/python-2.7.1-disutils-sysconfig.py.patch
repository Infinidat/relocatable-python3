diff --git a/Lib/distutils/sysconfig.py b/Lib/distutils/sysconfig.py
index ba48173..50adc3d 100644
--- a/Lib/distutils/sysconfig.py
+++ b/Lib/distutils/sysconfig.py
@@ -324,7 +324,11 @@ def parse_makefile(fn, g=None):
             done[k] = v.strip()
 
     # save the results in the global dictionary
-    g.update(done)
+    # do not override keys in the global dictionary (e.g. 'prefix')
+    for key,value in done.iteritems():
+        if key not in g:
+            g[key] = done[key]
+        
     return g
 
 
